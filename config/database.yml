default: &default
 adapter: postgresql
 encoding: unicode
 pool: 5

development:
 <<: *default

test:
 <<: *default


production:
 <<: *default
 database: <%= ENV["db_name"] %>
 username: <%= ENV["db_username"] %>
 password: <%= ENV["db_password"] %>
 host: <%= ENV["db_host"] %>