<div class="wrapper profile">
	<div class="row">
		<div class="col-sm-3">
				<% if current_user.image.present? %>
					<img src="<%=current_user.image%>" height="150px">
				<% end %>
			<h4><%= current_user.first_name + ' ' + current_user.last_name %></h2>
			<h5>Total Likes</h3>
		</div>
		<div class="col-sm-9">
			<% if @products.present? %>
			<%= render 'products/product_list' %>

			<% else %>
				<h3>Your liked products and collections will appear here.</h3>
				<button class="button big empty-state-button">Explore</button>
				</a>
			</div>

			<% end %>
		</div>

	</div>
</div>