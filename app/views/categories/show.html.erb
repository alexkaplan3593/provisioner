<div class="PageHeader">
	<div class="wrapper">
		<div class="">
			<div class="PageHeader-categoryName">
  			<%
  				if @category.present?
  					header = @category.category_name
  				
					end

  			%>
  			<%= header %>
			</div>
		</div>
	</div>

<%= render '/products/product_nav', locals: {products: @products} %>

<%= render '/products/product_tile', locals: {products: @products} %>
